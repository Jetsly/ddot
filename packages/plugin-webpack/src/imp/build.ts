class BuildCommand {
  // protected app: any;
  constructor() {
    // this.app = new Koa();
  }
  public async run() {
    // const port = await choosePort(DEFAULT_PORT);
    // this.app.listen(3000);
    console.log('BuildCommand');
  }
}
